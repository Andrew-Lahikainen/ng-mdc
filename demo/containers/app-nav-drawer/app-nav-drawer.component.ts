import template from './app-nav-drawer.component.html';
import './app-nav-drawer.component.scss';

export class AppNavDrawerContainer implements ng.IController {
  static readonly id = 'appNavDrawer';
  static readonly template = template;
}
