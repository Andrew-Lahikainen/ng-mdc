import { IdComponent } from '../../id-component';
import { HtmlCompilerComponent } from './html-compiler.component';

const component: IdComponent = {
  id: HtmlCompilerComponent.id,
  controller: HtmlCompilerComponent,
  bindings: HtmlCompilerComponent.bindings
};

export default component;
